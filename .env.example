# Event Horizon - Environment Variables (Day 1 MVP)
# NOTE: No paid API keys are required for Day 1 MVP!
# The system runs entirely local TF-IDF & heuristic matching.

BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000
FRONTEND_PORT=5173
